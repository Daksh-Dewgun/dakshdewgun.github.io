---
const projects = [
  {
    title: "Promissory Representation in Dominant Party Systems",
    description: "Exploring pledge-making strategies of Welsh political parties in their election manifestos using Artificial Intelligence methods.",
    status: "Ongoing"
  }
];
---

<section id="projects" class="projects">
  <div class="container">
    <h2>Current Projects</h2>
    <p class="intro">
      An overview of my ongoing and completed research projects, collaborations, and academic initiatives.
    </p>

    <div class="projects-list">
      {projects.map((project) => (
        <div class="project-card">
          <div class="project-header">
            <h3>{project.title}</h3>
            <span class={`status ${project.status.toLowerCase()}`}>
              {project.status}
            </span>
          </div>
          <p class="project-description">{project.description}</p>
          {project.link && (
            <a href={project.link} target="_blank" rel="noopener noreferrer" class="project-link">
              View Project
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
                <polyline points="15 3 21 3 21 9"></polyline>
                <line x1="10" y1="14" x2="21" y2="3"></line>
              </svg>
            </a>
          )}
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .projects {
    padding: calc(var(--spacing-unit) * 10) calc(var(--spacing-unit) * 4);
    background-color: var(--color-surface);
  }

  .container {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  h2 {
    margin-bottom: calc(var(--spacing-unit) * 3);
    color: var(--color-primary);
  }

  .intro {
    font-size: 1.125rem;
    color: var(--color-text-light);
    margin-bottom: calc(var(--spacing-unit) * 6);
    max-width: 800px;
  }

  .projects-list {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-unit) * 4);
  }

  .project-card {
    background-color: var(--color-background);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: calc(var(--spacing-unit) * 4);
    transition: all 0.3s ease;
  }

  .project-card:hover {
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    border-color: var(--color-accent);
  }

  .project-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: calc(var(--spacing-unit) * 2);
    margin-bottom: calc(var(--spacing-unit) * 2);
  }

  .project-card h3 {
    font-size: 1.5rem;
    color: var(--color-primary);
    margin-bottom: 0;
  }

  .status {
    display: inline-block;
    padding: calc(var(--spacing-unit) * 0.75) calc(var(--spacing-unit) * 2);
    font-size: 0.875rem;
    font-weight: 600;
    border-radius: 20px;
    white-space: nowrap;
  }

  .status.ongoing {
    background-color: #dbeafe;
    color: #1e40af;
  }

  .status.completed {
    background-color: #d1fae5;
    color: #065f46;
  }

  .project-description {
    color: var(--color-secondary);
    line-height: 1.7;
    margin-bottom: calc(var(--spacing-unit) * 3);
    font-size: 1.05rem;
  }

  .project-link {
    display: inline-flex;
    align-items: center;
    gap: calc(var(--spacing-unit));
    color: var(--color-accent);
    font-weight: 600;
    transition: gap 0.2s ease;
  }

  .project-link:hover {
    gap: calc(var(--spacing-unit) * 1.5);
  }

  @media (max-width: 768px) {
    .projects {
      padding: calc(var(--spacing-unit) * 6) calc(var(--spacing-unit) * 3);
    }

    .project-header {
      flex-direction: column;
      align-items: flex-start;
    }

    .project-card h3 {
      font-size: 1.25rem;
    }
  }
</style>
